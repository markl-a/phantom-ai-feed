<!--
Sample output, regenerable. Produced by the real pipeline against synthetic
local fixtures (no network, no real third-party data):

    python -m phantom_ai_feed.digest --use-stub \
        --feeds <synthetic-feeds.toml> --out <dir> --force

Feed names, links (example.org / example.com / example.net) and article text
are invented. The stub summarizer (no LLM / no API key) takes the first 1-2
sentences of each entry. Section headings come from the feed names in the
config; with a real GEMINI_API_KEY or `phantom exec` on PATH the per-entry
summaries are LLM-written instead. The "(stub-summarizer)" badge below records
which path produced this file.
-->

# phantom-ai-feed digest — 2026-06-14

_Generated by phantom_ai_feed.digest (stub-summarizer)._

## Sparse-MoE-routing  _(category: research)_
<file:///tmp/synth-feeds/research.xml>

- **[SparseMoE-7B: matching dense 13B quality at half the FLOPs](https://example.org/papers/sparsemoe-7b)**
  - A synthetic example paper. SparseMoE-7B routes tokens to 2 of 16 experts and reports parity with a dense 13B baseline on MMLU and GSM8K while cutting inference FLOPs by 48%.
- **[RetroEval: a reproducible offline benchmark harness for RAG systems](https://example.org/papers/retroeval)**
  - Synthetic example. RetroEval proposes a fully offline evaluation harness that decouples retrieval recall from generation faithfulness, and shows that answer-faithfulness correlates better with human ratings than retrieval recall alone.

## Quantization  _(category: blog)_
<file:///tmp/synth-feeds/blog.xml>

- **[Quantizing a 70B model to 4-bit without an accuracy cliff](https://example.com/blog/int4-no-cliff)**
  - Synthetic post. Walks through per-channel int4 weight quantization with a small calibration set, measuring perplexity drift at each layer.
- **[Building a daemon-friendly RSS pipeline in pure stdlib Python](https://example.com/blog/stdlib-rss)**
  - Synthetic post. Describes parsing RSS 2.0 and Atom with xml.etree, why the author avoids third-party feed libraries, and how to degrade gracefully when a single feed is unreachable.

## RAG-evaluation  _(category: community)_
<file:///tmp/synth-feeds/community.xml>

- **[Show: a local-first interview-prep tool for ML engineers](https://example.net/threads/local-interview-prep)**
  - Synthetic discussion thread. Author shares a weekend project that turns a week of AI news into spaced-repetition interview questions, kept entirely on-device.
